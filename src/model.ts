export interface VacancyCard {
	title: string,
	url: string,
	category: string,
	description: string[],
	status: string
}

export interface vacancyDescription {
	title: string,
	description: string[],
}